namespace Elementborn.Core
{
    /// <summary>The project version (semver). Kept in sync with the root <c>VERSION</c> file by
    /// <c>tools/bump-version.sh</c>, so the game can show its build (e.g. on an about/settings screen).</summary>
    public static class AppVersion
    {
        public const string Version = "0.1.0";
    }
}
